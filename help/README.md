# TicketBay help docs

Customer-facing help pages. The `search_docs` MCP tool searches these files
(`src/mcp/docs.ts`), so an agent answers policy questions from the policy, not
from memory. One topic per file; `##` headings split a page into the sections
the tool returns. Keep every number here in step with `src/domain/`.
