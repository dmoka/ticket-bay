# better-auth lists @tanstack/react-start as an optional peer, which pulls a
# vite 7+ peer that clashes with vitest 2 (vite 5). TicketBay never loads
# TanStack Start, so skip optional-peer resolution.
legacy-peer-deps=true
